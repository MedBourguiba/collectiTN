
{% extends 'backAdmin.html.twig' %}

{% block title %}CategorieMusee index{% endblock %}

{% block body %}
    <h1>CategorieMusee index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie_musee in categorie_musees %}
            <tr>
                <td>{{ categorie_musee.id }}</td>
                <td>{{ categorie_musee.name }}</td>
                <td>
                    <a href="{{ path('app_categorie_musee_show', {'id': categorie_musee.id}) }}">show</a>
                    <a href="{{ path('app_categorie_musee_edit', {'id': categorie_musee.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_categorie_musee_new') }}">Create new</a>
{% endblock %}
