
{% extends 'backAdmin.html.twig' %}
{% block title %}PieceMusee{% endblock %}

{% block body %}
    <h1>PieceMusee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ piece_musee.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ piece_musee.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ piece_musee.description }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ piece_musee.img }}</td>
            </tr>
            <tr>
                <th>PostedAt</th>
                <td>{{ piece_musee.postedAt ? piece_musee.postedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_piece_musee_index') }}">back to list</a>

    <a href="{{ path('app_piece_musee_edit', {'id': piece_musee.id}) }}">edit</a>

    {{ include('piece_musee/_delete_form.html.twig') }}
{% endblock %}
